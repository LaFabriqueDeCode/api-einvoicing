kafka:
  bootstrap_servers:
    - localhost:9092
  topic: einvoicing
  consumer_group_id: einvoicing-consumer

database:
  host: localhost
  port: 5432
  dbname: einvoicing
  user: einvoicing_user
  password: secret

providers:
  doxallia:
    base_url: "http://sebastien.ferrandez.free.fr/freepro/doxallia.php"
    timeout: 10
    auth:
      bearer_token: "dummy-token"